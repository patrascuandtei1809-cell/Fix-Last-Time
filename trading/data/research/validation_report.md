# 4h Edge — Rigorous Validation Report (NON-CANONICAL)

> ⚠️ **NOT A TRADABLE VERDICT. NOT THE SOURCE OF TRUTH.**
> The single canonical edge verdict is `research.py` → `data/research/latest.json`
> + the allowlist `data/research/validated_strategies.json`. That strict,
> multi-window/multi-symbol acceptance rule REJECTS every cell — including 4h V2
> (its aggregate across 90d+180d is negative). This document is a **non-canonical,
> exploratory robustness study** of individual candidates in ISOLATION. The
> "ROBUST/WEAK" labels below describe statistical stability of a single cherry-
> picked cell, NOT an approval to trade. **No strategy here is profitable or
> tradable under the canonical rule, and the allowlist is empty — the live bot
> stays auto-disabled.** Use this only as background for future research; never as
> a green light.

Generated by `trading/validate_candidates.py` (analysis only — does not touch the
live bot, dashboard, or allowlist). Cost model: 0.1%/side fee + 0.02%/side slip.
Monte Carlo: 5000 iterations (seeded, deterministic). Walk-forward: 5 sequential
out-of-sample folds (parameters are FIXED, so every fold is genuine OOS — this
measures stability across time, not curve-fitting).

## Verdicts

| candidate | history | trades | base exp | base PF | base Sharpe | **verdict** |
|-----------|---------|-------:|---------:|--------:|------------:|-------------|
| **V2 @ 4h ETH** | 2017–2026 (8.8y) | 295 | +0.837% | 1.40 | 2.43 | **🟢 ROBUST** |
| V2 @ 4h SOL | 2020–2026 (5.8y) | 200 | +0.751% | 1.23 | 1.24 | 🟡 WEAK |
| TrendPullback @ 4h SOL | 2020–2026 (5.8y) | 165 | +0.658% | 1.19 | 0.94 | 🟡 WEAK |

**These labels are isolated-cell robustness checks, NOT a tradable verdict.** Even
the strongest candidate is REJECTED by the canonical multi-window/multi-symbol rule
(`research.py`) — its aggregate across windows/symbols is negative after fees, so
it is not an approved or tradable edge and the allowlist stays empty.

## 🟢 ROBUST — EMA_MACD_RSI_VOLUME_V2 @ 4h ETH

- **Longer history:** +0.837%/trade over **295 trades / 8.8 years**, PF 1.40,
  Sharpe 2.43, total return +616%.
- **Walk-forward:** **5/5 folds positive** (incl. folds spanning the 2018 and
  2022 bear markets). Weakest fold still break-even-positive (+0.008%).
- **Monte Carlo:** P(expectancy>0) = **99.3%**; bootstrap 90% CI =
  [+0.278%, +1.389%] — **lower bound still positive**; bootstrap PF 5th pct 1.12.
- **Sensitivity:** positive under EVERY scenario — fee ±50%, slip ±50%, ATR
  SL ±25%, ATR TP ±25%. Worst case (ATR-TP −25%) still +0.369%/trade, PF 1.19,
  Sharpe 1.36. Not dependent on a narrow parameter set.
- ⚠️ **Drawdown risk is real:** reshuffled max drawdown median −43%, worst-5%
  −60%. The edge is positive but the equity path is volatile.

## 🟡 WEAK — V2 @ 4h SOL

Base is good and it survives fee/slip, but it fails two robustness bars:
- **Narrow-parameter dependence:** under a 25%-tighter stop (ATR-SL −25%) it
  collapses to **+0.018%/trade, Sharpe +0.04** — essentially break-even. A robust
  edge should not hinge on the exact stop distance.
- **Monte Carlo:** P(exp>0) = 89% (< 95%); bootstrap 90% CI lower bound
  **−0.228%** (includes negative); reshuffled worst-5% drawdown −78%.
- Walk-forward 4/5 folds positive (fold 1 slightly negative).

## 🟡 WEAK — Trend Pullback @ 4h SOL

- Survives all fee/slip/parameter perturbations (most stable to params of the three).
- But **Monte Carlo** P(exp>0) = 83%; bootstrap 90% CI lower bound −0.463%
  (includes negative); walk-forward fold 5 negative (−0.360%); worst-5%
  drawdown −78%. The point estimate is positive; the confidence interval is not.

## Bottom line

- In ISOLATION, V2 @ 4h ETH is the most statistically stable of the three (8.8y,
  walk-forward, Monte Carlo, fee/slip/parameter sensitivity). The two SOL variants
  are less stable — their Monte Carlo confidence intervals dip negative.
- **None of this is a tradable edge.** Under the canonical multi-window/
  multi-symbol acceptance rule (`research.py`), every one of these cells is
  REJECTED — single-cell robustness does not survive cross-window/cross-symbol
  aggregation after fees.

## Status (NOT a deployment recommendation)

The live auto-disable gate is **default-safe (allowlist empty → no auto-trading)**
and nothing here changes that. This report does NOT recommend trading any of these
candidates: the canonical verdict is "no after-fee edge," so the allowlist stays
empty and the bot stays auto-disabled. Treat everything above as exploratory
background only — a fresh, genuinely different edge source (funding/basis,
cross-exchange, on-chain/news) would have to pass the canonical rule before any
strategy could be added to the allowlist.
